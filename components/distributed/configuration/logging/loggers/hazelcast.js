
configuration.logger({
	name: 'com.hazelcast',
	level: 'info'
})

configuration.logger({
	name: 'com.hazelcast.config',
	level: 'info'
})

configuration.logger({
	name: 'com.hazelcast.system',
	level: 'info'
})

configuration.logger({
	name: 'com.hazelcast.impl',
	level: 'info'
})
